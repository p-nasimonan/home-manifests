Argo CD manifests

このフォルダには Argo CD 用の Application 定義と bootstrap 手順を入れています。

- `applications/`: Argo CD の `Application` リソース（sealed-secrets, example-app など）。
- `sealed-secrets/`: sealed-secrets セットアップ手順とインストールガイド。
