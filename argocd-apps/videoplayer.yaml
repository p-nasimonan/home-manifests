apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: videoplayer
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/p-nasimonan/home-manifests.git
    targetRevision: HEAD
    path: apps/videoplayer
  destination:
    server: https://kubernetes.default.svc
    namespace: wolweb
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
