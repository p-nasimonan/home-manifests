pre-commit:
  parallel: true
  commands:
    check-secrets:
      run: npm run check:secrets

# Optional: run additional checks on pre-push (uncomment if desired)
#pre-push:
#  parallel: true
#  commands:
#    run-pre-push:
#      run: |
#        pre-commit run --hook-stage pre-push --all-files
